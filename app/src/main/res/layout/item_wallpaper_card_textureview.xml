<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="10dp"
    android:background="@android:color/transparent">

    <!-- 🌈 Contenedor general con sombra elegante -->
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardCornerRadius="18dp"
        app:cardElevation="10dp"
        android:layout_margin="4dp"
        android:background="@android:color/transparent"
        android:foreground="?android:attr/selectableItemBackground">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="#161926"
            android:padding="0dp">

            <!-- 🌟 PREVIEW + MARCO NEÓN -->
            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="220dp"
                android:layout_margin="18dp"
                android:layout_gravity="center">

                <!-- Shader neón (solo alrededor de la imagen) -->
                <com.secret.blackholeglow.opengl.NeonBorderTextureView
                    android:id="@+id/neon_border_effect"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:alpha="1"
                    android:visibility="visible"
                    android:layout_gravity="center" />

                <!-- Imagen centrada, con esquinas redondeadas y sombra -->
                <ImageView
                    android:id="@+id/image_preview"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="centerCrop"
                    android:background="@drawable/card_background"
                    android:layout_margin="12dp"
                    android:elevation="4dp"
                    android:contentDescription="@string/preview_image_desc"
                    app:cornerRadius="16dp"
                    android:clipToOutline="true"/>
            </FrameLayout>

            <!-- Título -->
            <TextView
                android:id="@+id/text_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/wallpaper_title"
                android:textColor="@android:color/white"
                android:textStyle="bold"
                android:textSize="20sp"
                android:gravity="center"
                android:paddingTop="2dp"
                android:paddingBottom="0dp"/>

            <!-- Descripción -->
            <TextView
                android:id="@+id/text_description"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/wallpaper_description"
                android:textColor="#BBBBBB"
                android:textSize="14sp"
                android:gravity="center"
                android:paddingBottom="4dp"
                android:paddingTop="0dp"
                android:paddingLeft="18dp"
                android:paddingRight="18dp" />

            <!-- Botón Aplicar -->
            <Button
                android:id="@+id/button_apply"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_marginTop="8dp"
                android:layout_marginBottom="14dp"
                android:backgroundTint="@color/purple_500"
                android:textColor="@android:color/white"
                android:text="@string/apply"
                android:elevation="4dp"/>
        </LinearLayout>
    </androidx.cardview.widget.CardView>
</FrameLayout>
